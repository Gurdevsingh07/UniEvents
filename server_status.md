# Server Status

Servers are currently **RUNNING**.

- **Backend API**: http://localhost:8080
- **Frontend App**: http://localhost:5173

Servers were started using `npm run dev` in the frontend directory (which starts both backend and frontend).
Confirmed listening on ports 8080 and 5173.
